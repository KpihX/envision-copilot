# Envision Benchmark (`src/envision_benchmark`)

**Role**: Evaluation Framework.

## Features
- **Runner**: Executes questions against `envision_copilot`.
- **Judge**: Uses `llms` (LLM-as-Judge) to score answers.

## Usage
```bash
uv run benchmark
```

## Input
- `questions.json`
